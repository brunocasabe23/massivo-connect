# Puerto para el servidor backend
PORT=5000

# Variables de conexión a la base de datos PostgreSQL
DB_USER=postgres
DB_HOST=localhost
DB_NAME=gestion_compras_db
DB_PASSWORD=your_db_password # ¡IMPORTANTE: Cambiar esto!
DB_PORT=5432

# Secret para JWT (JSON Web Token) - ¡IMPORTANTE: Cambiar por una cadena larga y aleatoria!
# Se recomienda generar una con un generador de claves seguras (ej: openssl rand -base64 32)
JWT_SECRET=una_cadena_secreta_muy_larga_y_aleatoria_aqui 

# Otras configuraciones (ej: email service, etc.)